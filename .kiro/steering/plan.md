---
inclusion: always
---

# Архитектурный план проекта ANIMATIONAPP

## Обзор

Проект разрабатывается в 3 этапа:
1. **Этап 1 (текущий):** Редактор пиксельарта с инструментами рисования
2. **Этап 2 (будущий):** Полигонизация слоёв в меши кастомным образом + самописная скелетная анимация
3. **Этап 3 (будущий):** Настройка и экспорт правил роста и эволюции пикселей (клеточные автоматы) и слоёв изображения для игры

Архитектура спроектирована с учётом будущего расширения.

---

## Принципы архитектуры

1. **Слабая связанность** — компоненты общаются через EventBus и Services
2. **Высокая связность** — каждый модуль отвечает за одну область
3. **Модульность** — легко добавлять новые этапы/инструменты/функции
4. **Тестируемость** — чистая логика в RefCounted классах
5. **Расширяемость** — архитектура готова к этапам 2 и 3

Если в процессе разработки любой из этих принципов нарушается, уведомляй пользователя и предлагай корректировки плана.

---

## Текущие задачи

> Данный раздел корректируется в процессе реализации, затем дополняется пользователем.
> Для общих пунктов плана эмодзи 🎯, для выполненного пункта плана эмодзи ✅. Для локальных задач в плане один таб + эмодзи 📌. Для выполненных задач эмодзи ✔️ для текущей задачи на очереди ставь 🕊️.

> Разноцветными кругами эмодзи (⚫🔴🟠🟡🟢🔵🟣⚪) можно при необходимости указывать связи (подвязывать пункты), без которых пункт не может быть реализован. Не злоупотреблять. Уместно.

> UI задачи предоставлены пользователю

✅ Доделать tool систему 

    ✔️ Изменение размера кисти/ластика через экшены ("add", "sub")
    ✔️ BrushTool: возможность рисовать secondary_color через правую кнопку (action "cancel")
    ✔️ BrushTool: переключатель формы кисти (квадрат/круг)
    ✔️ FillTool с переключателем contiguous 🔵
        - Flood fill алгоритм (BFS, строковые ключи для visited)
    ✔️ Система настроек инструментов (ToolService.tool_properties + PropertiesPanel)
    📌 SelectionTool с гизмо внутри canvas_renderer
        - Пайплайн: выделяем (минимум от одного пикселя) область. Курсор не видно, только гизмо. После выделения, курсор появляется. При наведении на выбранную область смена курсора на ToolType.Type.DRAG. При перетаскивании области, все внутренние пиксели этой области двигаются с ней. Экшн "deselect" снимает выделение.
    📌 PickerTool (может даже не Tool) как неявный инструмент, без кнопки.
        - Пайплайн: зажатии action "picker" скрывает курсор, рисует небольшой гизмо круг тончайшей обводки вокруг позиции мыши, а снизу под ним рисует превью выбираемого цвета. Цвет выбирается с глобального вьюпорта, не с холста. При клике action "action" либо "cancel" цвет устанавливается в AppState primary_ и secondary_color соответственно. По отпускании action "picker", просто возвращается предыдущее состояние курсора/инструмента.

🎯* Убрать все уже ненужные print() дебаги

🎯 Undo/redo

    📌 Для инструментов (нарисовали, стёрли, залили, выделили, переместили)
    📌 Для слоёв (добавили, удалили, видимость, перемещение)
    

🎯 Tool properties элементы в правой верхней боковой панели

    📌 UI компонент простой переключатель с надписью
    📌 UI компонент IconButton, как ToolButton. Иконка щётки.
    📌 Логика
        - Brush/Eraser: переключатель формата кисти: круг, квадрат
        - Fill: переключатель contiguous 🔵
        - ClearButton: очищает текущий слой

🎯 Камера

    📌 Лимиты зума (количественные от x1 до x10)
    📌 Лимиты смены положения камеры (при зуме или перетаскивании колёсиком)
        - Чтобы холст не мог слишком вылезать за пределы видмости

🎯 Общее

    📌 UI компонент иконка-переключатель
    📌 Кнопка видимости каждого слоя



---

